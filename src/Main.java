public class Main {
    public static void main(String[] args) {

        double x = Math.random();
        double y = Math.random();

        //           (x-0.5)^2 + (y-0.5)^2 <= 0.25
        // x^2 - x + 0.25 + y^2 - y + 0.25 <= 0.25
        //              x^2 - x + y^2 - y <= -0.25

        int hit, num;

        //4.0 * hit / num;

        // will have to use for loops and ifs
    }
}